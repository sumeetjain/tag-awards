<h1>Final Ballot</h1>

<p>Check below for the final ballot. Once you submit the ballot, you cannot modify it.</p>
<p>You can also vote for five new members for our 2016-2017 Theatre Arts Guild Board of Directors. This is a separate form that is not connected to this ballot.<br><a target="_blank" href="http://goo.gl/forms/Fb1FTVCmfo6KGciJ2 ">Click here to vote for Board of Directors (opens a new window).</a></p>

<% if @ballots_ready %>
    <% if !current_user.has_submitted_final_ballot? %>
        <hr>
        <p>Your ballot has been submitted. Thanks again!</p>
    <% else %>

        <%= form_for :ballot, {url: ballots_path, html: {}} do |f| %>

            <div class="clear">
                <% @awards.each do |award| %>
                    <div class="finalBallotSection">
                        <h2 class="finalBallotSection__award">
                            <%= award.award_name %>
                        </h2>

                        <% award.ballot_items.each do |ballot_item| %>
                            <label class="finalBallotSection__label">
                                <%= f.radio_button award.id, ballot_item.id %>
                                <%= ballot_item_info(ballot_item) %>
                            </label>
                        <% end %>
                    </div>
                <% end %>
            </div>

            <%= f.submit %>
        <% end %>

    <% end %>
<% else %>
    <p>Final Ballots are not yet available. Please check back after you've received notification that nominations have been tallied.</p>
<% end %>