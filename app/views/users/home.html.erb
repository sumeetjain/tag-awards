<html>
<head>
    <title>User Dashboard</title>
</head>
<body>
  <div class="clear title">
    <div class="col-12 title-box">
      <h2>Show Attendance 2016</h2>
    </div>
  </div>

  <div class="clear content">
    <div class="col-12 content-box">
      <div class="clear">
        <div class="col-4">
          <% if current_user.viewings != nil %>
          <% current_user.viewings.each do |view|%>
              <p><%= view.play.title%> </p>
            <% end %>
            <% else %>
            No Viewings Recorded Yet.
            <% end %>
        </div>

        <div class="col-4">
          <% if current_user.viewings != nil %>
          <% current_user.viewings.each do |view|%>
              <p> <%= view.play.theater.name %> </p>
            <% end %>
            <% else %>
            No Viewings Recorded Yet.
            <% end %>
        </div>

        <div class="col-4">
          <% if current_user.viewings != nil %>
          <% current_user.viewings.each do |view|%>
              <p> <%= view.date %> </p>
            <% end %>
            <% else %>
            No Viewings Recorded Yet.
            <% end %>
        </div>

      </div>
    </div>
  </div>


  <div>
    <a class="button" href="/viewings/new" target="_self">add show</a>
    <a class="button" href="/viewings/index" target="_self">delete show</a>
  </div>

</body>
</html>