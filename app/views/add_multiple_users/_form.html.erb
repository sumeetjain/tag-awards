<%= form_for(@add_multiple_user) do |f| %>
  <% if @add_multiple_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@add_multiple_user.errors.count, "error") %> prohibited this add_multiple_user from being saved:</h2>

      <ul>
      <% @add_multiple_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
